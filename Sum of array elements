#include <stdio.h>

int main()
 {
  int *arr;
  int sum=0;
  arr=(int*)malloc(5*sizeof(int));
  printf("Enter numbers");
  for(int i=0;i<5;i++)
    {
      scanf("%d",&arr[i]);
      sum=sum+arr[i];
    }
  printf("Sum is %d",sum);
  return 0;
}
